apiVersion: v1
kind: ConfigMap
metadata:
  name: chatapp-config
  namespace: chatapp
data:
  MYSQL_DATABASE: chatapp
  SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/chatapp?useSSL=false&serverTimezone=UTC
  SPRING_DATASOURCE_USERNAME: root
